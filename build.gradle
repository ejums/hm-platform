plugins {
    id "org.sonarqube" version "3.4.0.2513"
}

sonarqube {
    properties {
        property "sonar.projectKey", "ejums_hm-platform"
        property "sonar.organization", "ejums"
        property "sonar.host.url", "https://sonarcloud.io"
    }
}